import state from "../states/UIState";
import actions from "../actions/UIAction";
import mutations from "../mutations/UIMutation";
import getters from "../getters/UIGetters";

const UIModule = {
  state,
  mutations,
  actions,
  getters,
};

export default UIModule;
